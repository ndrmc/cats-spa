<div class="right_col" role="main">
  <div class="">
    <div class="page-title">
      <div class="title_left">
        <h3>Stock Receipt List</h3>
      </div>

      <div class="title_right">
        <div class="col-md-5 col-sm-5 col-xs-12 form-group pull-right top_search">
          <a href="/receipts/new" class="btn btn-primary pull-right">New Receipt</a>
        </div>
      </div>
    </div>

    <div class="clearfix"></div>

    <div class="row">
      <div class="col-md-12 col-sm-12 col-xs-12">

        <div class="x_panel">
          <div class="x_title">
            <h2>Stock Receipt</h2>
            <div class="clearfix"></div>
          </div>
          <div class="x_content">
              <table class="table basic-datatable">
                <thead>
                  <tr>
                    <th>GRN No.</th>
                    <th>Receipt Date</th>
                    <th>Store</th>
                    <th>Received By</th>
                    <th>Posted</th>
                    <th>Tasks</th>
                  </tr>
                </thead>
                <tbody>
                  {{#each receipts}}
                    <tr>
                      <td>{{this.grnNo}}</td>
                      <td>{{this.receivedDate}}</td>
                      <td>{{this.storeName}}</td>
                      <td>{{this.storekeeperName}}</td>
                      <td>
                        {{#if this.draft}}
                          Draft
                        {{ else }}
                          Yes
                        {{/if}}
                      </td>
                      <td>
                        {{#if this.draft}}
                          <a href="#">Open</a> &middot; <a href="#">Post</a> &middot; <a href="#">Delete</a>
                        {{ else }}
                          <a href="#">Open</a>
                        {{/if}}
                      </td>
                    </tr>
                  {{/each}}
                </tbody>
              </table>
          </div>
        </div>

      </div>
    </div>
  </div>
</div>